Act as lead developer implementing issue #{item} directly.

The architect's design is available:
{{handoff:architect}}

This issue is too complex for junior-dev but does not require a separate scaffolding
phase. Implement the solution directly, following the architect's recommendations.

Read the issue and any relevant source files, then implement the changes. Run tests
if a test command is available to confirm correctness.
