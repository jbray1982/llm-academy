Act as junior developer implementing issue #{item} directly.

The architect's design is available:
{{handoff:architect}}

This issue is simple and fully specified. Implement the changes as described, following
existing code conventions. If you encounter anything unclear, prefer the conservative
interpretation (do less, not more) and note it in your output.

Run tests if available to confirm your changes are correct.
